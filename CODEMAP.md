# CODEMAP.md

A short structural map of the actual codebase — not what the app is (`spec.md`), not the build order (`PLAN.md`), not what happened (`DEVLOG.md`): where things physically live. Updated on real structural change (a new major component, a folder reorganization, a new shared module), not every session — same discipline as `spec.md`, not a diary.

Added on external review, Sept 2026: without this, every session re-derives the codebase's shape by reading around it, and across many sessions that's how two components quietly end up doing the same job.

---

## Top-level structure

React + Vite app, plain JavaScript (no TypeScript — not asked for, and Markus never reads the code directly). Built with `npm run build` into `dist/` (gitignored), deployed to GitHub Pages by `.github/workflows/deploy.yml` on every push to `main`.

- `index.html` — Vite entry HTML; loads `src/main.jsx`.
- `vite.config.js` — React plugin, Tailwind v4 (`@tailwindcss/vite`), and `vite-plugin-pwa` (see below). `base: '/Geld/'` because the app is served from a GitHub Pages project site, not its own domain.
- `src/main.jsx` — app entry point; registers the service worker (`virtual:pwa-register`) and mounts `<App />`.
- `src/App.jsx` — the app shell: sign-in/sign-out, the auth-gate screens, and the header (year selector lives inside `Konten.jsx` for now, not here — §1b.2a's "single shared header" isn't built yet). Renders `Konten` as the default/only real screen; `ImportScreen` is reachable via a "Datenimport" toggle button in the header, not a real nav shell — see PLAN.md Phase 2's note on why that's scheduled for later, once a third real screen (Verlauf) exists to make a toggle insufficient. (Phase 2, not Phase 3 — Sept 2026, Markus swapped the two so Verlauf/Planung comes before Konten's CSV import.)
- `src/firebase.js` — Firebase `app`/`auth`/`db` initialization. Firestore is initialized with `persistentLocalCache` + `persistentMultipleTabManager` (spec.md §1a's "offline cache is the real data store, not a bolt-on").
- `src/firebaseConfig.js` — the public Firebase project config object (not secret, safe to commit — see the comment in the file).
- `src/lib/authReady.js` — `waitForInitialAuthState()`, the bounded (4s) timeout around Firebase Auth's own init calls (spec.md §1a's cold-start-stall mitigation). Any future code that needs "is there a signed-in user yet" at startup should use this rather than reading `auth.currentUser` directly or assuming `onAuthStateChanged` fires promptly.
- `src/sw.js` — hand-written service worker source, built via `vite-plugin-pwa`'s `injectManifest` strategy (**not** `generateSW`, which is the default and does *not* give network-first navigation — a real footgun already hit once during Phase 0: the option is `strategies` (plural), and a `strategy` typo silently falls back to `generateSW` with no error). Implements: precache-and-route from the real build manifest, and a `NavigationRoute` wrapped in `NetworkFirst` (2.5s timeout, `app-shell` cache) so navigation never serves a stale cached shell while online (spec.md §1a).
- `src/index.css` — Tailwind import, the semantic color tokens from spec.md §1b.4 (as CSS custom properties, light + `prefers-color-scheme: dark`; manual override switch not built yet — lands with the Settings screen), self-hosted Inter/JetBrains Mono via `@fontsource/*` (not a Google Fonts CDN link — offline-first means no runtime dependency on a third-party host for something as basic as page text), and the `overscroll-behavior: none` / `touch-action: pan-y` fix from spec.md §1.
- `public/icons/` — placeholder PWA icons (flat-color circle mark, programmatically generated, not a real design) — swap these out whenever real icon design happens; nothing depends on their current appearance.
- `firebase.json` / `.firebaserc` / `firestore.rules` / `firestore.indexes.json` — Firestore-only Firebase config (no `hosting` key — GitHub Pages serves the app, not Firebase Hosting, per spec.md §1b.1). `firestore.rules` is live with the real single-admin-UID rule (§1a/§1b.1) — Markus's actual Google account UID, not a placeholder — confirmed working via real signed-in usage throughout Phase 1a/1b (the 2026-09-22 import, and all of Konten's real editing since).

### `migration/` — one-off 2025 + 2026 import from the Gsheets (PLAN.md Phase 1a)

Not part of the app bundle. Python scripts that turn Markus's yearly "Geld 2025" / "Geld 2026" Google Sheets into Firestore-shaped JSON, each with built-in checks against the sheet's own totals that fail loudly on any mismatch.

- `migration/seed/` — **committed**, non-personal static data transcribed from spec.md: `accounts.json` (§2.2 — the seven `receivable` accounts carry `reportingGroup: "Außenstände"`, revised Sept 2026), `categories.json` (§2.4), `tags-allocation.json` (the 7 allocation tags, §2.5). **A change here needs re-running `ImportScreen` for that file to actually reach Firestore** — editing the seed JSON alone doesn't touch already-imported data.
- `migration/transform-transactions.py [year]` — `Konten` tab → that year's transactions; for 2025 also the one-time Jahresabschluß opening balances. Later years skip their sheet's own opening rows and are checked against the full history (Jahresabschluß + every year so far), so the later sheet's header must come out of the app's own carry-over; known carry-over differences are either booked (`CARRYOVER_CORRECTIONS`) or explained by an earlier-year `CORRECTIONS` entry. Core rule, verified against the sheet: every row changes only its own Konto (`Σ Teilwert per Konto` is the sheet's header formula; `Wert` never counts); transfers appear as two rows and are paired globally, unpaired rows stay single-sided. Confirmed data-entry typos live in its `CORRECTIONS` table. Checks: all 18 account balances, all 7 allocation-tag totals, all 59 claims (open/settled, per receivable account) against the sheet.
- `migration/transform-budgets.py [year]` — `Verlauf` tab → Plan0 + Plan1 budgets and their breakdown-line grouping tags (Prog is not imported — computed live). Checks: group totals per month against the Verlauf summary rows, savings actuals per month against the Prog rows.
- `migration/out/` — **gitignored**, generated output (real personal data). **This repo is public (GitHub Pages) — nothing personal may ever be committed.** The generated files will reach Firestore via the app's one-time import screen (file picker, not bundled into the public app).

To regenerate: export each year's sheet from Markus's Google Drive as `.xlsx` (the Drive connector's download with the xlsx export type keeps every tab — CSV export only gives the first tab), save it at the path in each script's `SRC` constant, then run in this order: `transform-transactions.py 2025`, `transform-transactions.py 2026` (needs the 2025 output), then `transform-budgets.py 2025` and `2026` (each reads its year's transactions for the savings check). Needs `openpyxl`.

## Screen-to-component map

- `src/Konten.jsx` — the main ledger grid (PLAN.md Phase 1a). Loads `accounts`/`categories`/`transactions` live via `onSnapshot` (all-time, not year-scoped — `balance()` needs the full history, §2.1/§2.3/§2.8), computes a year list from the loaded transactions' own dates, and renders a pinned per-`reportingGroup` balance panel (Barkonten/Sparkonten/Geldanlage/Außenstände, via `jahresende()`) above an AG Grid (`ag-grid-community`/`ag-grid-react`, Theming API — `themeQuartz`, Community-tier `AllCommunityModule`) listing the selected year's transactions. **Confirmed against the real 2025 import: every account's Jahresende matches the Gsheet's own closing balance** — Phase 1a's actual testable deliverable (PLAN.md).
  - **The grid follows system dark mode now, like the rest of the app** — `src/lib/gridColorScheme.js`'s `syncAgGridColorScheme()` (called once, at module load). `themeQuartz` ships light/dark params out of the box (its `colorSchemeVariable` part), but they're gated behind a `data-ag-theme-mode` attribute on `<html>`/`<body>` that nothing was ever setting, so the grid always rendered in its light default regardless of the app shell correctly following `prefers-color-scheme` via CSS (Markus: illegible in dark mode). Verified visually with a standalone Playwright harness (not just read from source) before shipping — AG Grid's own dark palette, not tied to this app's own CSS tokens, so it won't match exactly, but reads correctly and legibly. Reusable as-is for a future AG Grid screen (Verlauf, §1b.3) — idempotent if called again.
  - **Column order matches spec.md §3a exactly:** Datum → Konto → Empfänger → Betrag → Kategorie → Unterkategorie → Details → Tags (plus an unlabeled, pinned-right delete column, not part of the spec'd order). Konto is one merged column showing the primary account with an arrow to the second one for a transfer (not Konto1/Konto2 — see spec.md §3a's note on why). Kategorie (the parent group) is derived from Unterkategorie's `parentCategoryId`, never stored separately. `signedFor(tx, accountId)` — the same magnitude-by-position rule `balance()` uses — powers the Betrag column, the Konto arrow's direction, and the account filter below.
  - **Account filter** (a `<select>` grouped by reportingGroup via `<optgroup>`, plus clicking an account in the pinned panel): matches every transaction where the chosen account is either side, and re-displays Konto/Betrag from that account's own perspective (the other account, and the amount signed relative to it). The pinned panel's own account buttons have their own roving keyboard navigation (below), richer than the plain `<select>`.
  - **Manual entry is fully built**: "+ Neue Buchung" creates a blank row below the selected one (pre-filled with the active account filter, if any) and cell-focuses its Datum cell without starting to edit it; every column is editable in place via one `setDoc` full-overwrite per commit (`persistTx`). Deleting is a pinned trashcan (or the keyboard Delete key) with a two-click arm/confirm — the armed row tints pale red (`--color-alert-tint`) and Escape discharges it. This is a real hard `deleteDoc`, not spec.md §2.9a's planned soft-delete-with-recovery-window (Phase 1b) — Firestore's PITR (7-day window, since Phase 0) is the actual safety net until then.
  - **Split transactions (§3a's auto-remainder mechanism, Phase 1b) are built.** AG Grid Community has no master/detail row support (that's Enterprise-only) — `displayRows` (a `useMemo` flat-mapping `rows`) is the Community-tier equivalent: an expanded split transaction's `lines[]` get interleaved right after their parent as synthetic rows (`{ id, __isLine: true, __parent, __lineIndex }`, `__parent` a live reference into `rows`), and every column def branches on `p.data.__isLine` to render/edit differently for a line row vs. its parent. A pinned-left "split" column (`colId: 'split'`) handles expand/add: ✚ on an unsplit parent starts the first split and auto-expands; a chevron toggles `expandedIds` once split (`>1` lines); the *last* line (the live remainder) also gets ✚ to split further. Appending a blank line is the same operation whether it's the first split or splitting an existing remainder further — no special-casing needed, since the previously-last line simply becomes directly editable and the newly-appended one becomes the new remainder. **Removing a line goes through the trashcan column instead** (Markus) — it arms/confirms/red-tints exactly like deleting a whole transaction (`handleDeleteClick` now takes the row itself and branches: `removeLine` for a line row, `softDeleteTx` for a parent — see soft-delete below, PLAN.md Phase 1b), not a separate instant-remove affordance; `confirmDeleteId` doesn't care whether the id it holds belongs to a parent or a line row.
  - **Soft-delete with a recovery window (spec.md §2.9a's layer 4, PLAN.md Phase 1b — built Sept 2026).** Deleting a whole transaction (the trashcan's confirm click, or `Konten.jsx`'s Delete-key handler) now calls `softDeleteTx(tx)` — `persistTx({ ...tx, deletedAt: Date.now() })` — instead of a real `deleteDoc`. A line's own removal (`removeLine`) is unaffected — that's still an immediate, real edit to the parent's `lines[]`, never soft-deleted itself; only a whole-transaction delete goes through this layer.
    - **`activeTransactions`** (`useMemo(() => transactions.filter((t) => !t.deletedAt), [transactions])`) is what every aggregation actually reads now — `jahresende`/`tagJahresende`/`tagFilterTotal` calls in the pinned panel and the toolbar's "Angezeigt:" readout, plus `usedTagValues`/`recentTagValues`'s usage-derived tag suggestions. `transactions` itself (raw, including soft-deleted rows) is still what `rows` (the grid's own row source) and `years` (the year-picker) read directly — a soft-deleted row needs to stay reachable there for recovery, and a year shouldn't vanish from the picker just because everything in it happens to be currently deleted.
    - **`showDeleted`** state, off by default, behind a "Kürzlich gelöscht" checkbox in the toolbar (spec.md's own "existing filter bar" placement) — `rows`' own filter chain gets one more clause, `showDeleted || !t.deletedAt`, so a soft-deleted row reappears in the grid (interleaved by date, same as any other row) only while the toggle is on, never changing what `activeTransactions` (and therefore every balance) already excludes regardless of the toggle. Its own explanatory `title` tooltip (added, then removed the same round — Markus) is gone; the checkbox is plain text now.
    - **Manual "empty the trash" (Sept 2026, Markus: "i need a function to permanently delete all 'kürzlich gelöscht' rows... in case i have to delete a large number of rows for any reason")** — a 🗑 button appears right next to the checkbox label, only while it's checked, opening a confirmation modal (`confirmPurgeOpen` state; a plain `position: fixed` overlay, not a portal — renders correctly regardless of nesting depth) rather than acting on click alone. Confirming calls `purgeAllDeleted()`, which `deleteDoc`s *every* currently soft-deleted transaction directly — not scoped to the selected year (a real "clean up a large number of rows" could span years) — distinct from the automatic ~week-later purge below, which only ever catches up on its own schedule rather than on demand. Escape also closes the modal (its own small effect, same pattern as the armed-delete Escape discharge above).
    - **Visually distinct, restorable through the same two-click arm/confirm as delete** (revised Sept 2026, Markus: "assign the same DEL button shortcut to the deleted row in order to undelete it... deleting and undeleting the same row takes 4 presses of DEL") — `handleDeleteClick` now branches a *third* way on confirm: `removeLine` for a line row, `restoreTx(row)` (`persistTx({ ...tx, deletedAt: null })`) when `row.deletedAt` is set, `softDeleteTx(row)` otherwise. One shared `confirmDeleteId`/arm-then-confirm flow covers both directions symmetrically — the DEL key (`onCellKeyDown`) and the delete column's own button both route through it unchanged. The cellRenderer shows ↺ (not deleted, not armed → 🗑; armed, either direction → ⚠︎; deleted, not armed → ↺), with a matching title ("Nochmal klicken zum Löschen" vs. "...zum Wiederherstellen"). `getRowStyle` dims a deleted-but-unarmed row (`opacity: 0.5`) — a deliberately different visual choice from the *armed* tint (checked first, so it still wins while armed, either direction) — that's "about to delete/restore," this is "already deleted, browsing." **The armed tint itself is now direction-aware (Sept 2026, Markus: "instead of turning red, turn the row in a green tint when staged for undeletion")** — pale red (`--color-alert-tint`) while arming a *delete*, pale green (`--color-income-tint`, new token, same light/dark-mode pattern as `--color-alert-tint` in `index.css`) while arming a *restore* (`p.data.deletedAt` already set at the moment of the first press) — undoing a delete is a positive action, not a destructive one, so it reads differently from the "about to delete" warning color.
    - **Fully locked down except restoring, once deleted** (Sept 2026, Markus: "i should not be able to create split rows on a deleted item or interact in any way with it... except undelete it") — `isRowDeleted(data)` (a small helper reading `data.deletedAt` directly, or `data.__parent.deletedAt` for a line row of an already-split, now-deleted transaction) gates every column's own `editable`, so AG Grid never even opens a cell editor on a deleted row. The split column's own ✚ buttons (start a split / split further) render as nothing (`return null`) rather than disabled, on both the parent and last-line branches; the Ctrl+T global shortcut short-circuits the same way (still swallows the keypress via `preventDefault`/`stopPropagation`, just skips calling `addSplitLine`). The chevron toggle to expand/collapse an *already*-split deleted transaction's own lines is deliberately left working — viewing existing lines isn't an edit, only creating/changing them is what's locked.
    - **The actual purge** — "recoverable for roughly a week... before an actual purge" needed something to make that real, not just a permanently-growing pile of soft-deleted docs. No backend cron exists (GitHub Pages + Firestore, no server), so a `useEffect` keyed on `transactions` runs opportunistically instead, whenever transactions load or change: any row with `deletedAt` older than 7 days gets a real `deleteDoc`, guarded by a session-scoped `purgedIdsRef` (a `Set` of already-purged ids) so a `deleteDoc` isn't redundantly reissued for the same row while Firestore's own `onSnapshot` hasn't yet confirmed the previous one and dropped it from `transactions`.
    - **Verified via a disposable Playwright harness against a mocked `firebase/firestore`** (an in-memory store + pub/sub standing in for `onSnapshot`/`setDoc`/`deleteDoc`, wired in through a temporary `vite.config.js` alias gated behind an env var, both reverted before commit) — the real `Konten` component rendered directly, no live Firestore access needed. Confirmed: deleting excludes a transaction from the pinned panel's balance immediately and hides it from the grid; toggling "Kürzlich gelöscht" on reveals it greyed with a working restore button while the panel balance stays unchanged (still excluded); restoring brings the balance and the row's normal appearance back; a synthetic row seeded with an 8-day-old `deletedAt` is actually removed by the purge effect.
    - **Ctrl/Cmd+T ("Teilen") triggers the same add/split-further action as the ✚ button**, for whichever transaction the cursor currently sits on (its parent row or any of its own line rows) — reads the focused cell's row via `api.getFocusedCell()`/`getDisplayedRowAtIndex()`, resolves `__isLine ? __parent : row`, then calls `addSplitLine` exactly like the button would. Both the button and the shortcut land the cursor specifically on line index 0's own Betrag cell afterward (`pendingFocusColRef = 'betrag'`, `pendingFocusLineIndexRef = 0`, always the *first* line literally, even when splitting further deep into an already-multi-line split) — the natural next step either way is typing how much that first piece is. Betrag has an explicit `colId: 'betrag'` now so this can target it reliably.
      - **Was Ctrl+Enter originally** — registered on `window` with capture phase and `stopPropagation()` to win a real race against AG Grid's own native keydown listener on the row container (`RowContainerEventsFeature`, confirmed in its bundled source; same underlying lesson as `Listbox.jsx`'s Enter fix, Session 21: a page-level handler can't out-run a closer native one without running first). That fix was necessary but not sufficient — Markus reported it still didn't fire, most likely because Enter itself is also one of the most universally browser/OS-reserved key combinations there is on some platforms (iPadOS's own external-keyboard handling included), meaning the browser/OS can swallow the keypress before it's ever dispatched to the page's JavaScript at all — a layer no in-page fix can reach. Reassigned to Ctrl+T instead (Session 31) — the capture-phase/stopPropagation fix is kept regardless (still correct, still needed for the in-page AG Grid race, harmless either way), even though T isn't a key AG Grid's own core claims. **T itself is a real gamble in the same category** — it's *the* nearly-universal "open new tab" browser shortcut, arguably reserved even more aggressively than Enter — so if it silently fails too, that's the same browser/OS-level obstacle, not this code.
    - **A line row's synthetic id bakes in its parent's current line *count*, not just its own index** (`${tx.id}::line::${i}::${tx.lines.length}`) — a line has no stable id of its own in the schema (§2.6). Index-alone ids meant removing a non-last line shifted every later line's index, so a surviving line inherited a *different* line's old id under AG Grid's own id-based reconciliation, which can't tell "this row moved" from "this id now holds different content" — a stale leftover row after deleting a non-last line (Markus). Baking the count in forces a full rebuild of that transaction's line rows on any add/remove, while keeping ids (and smooth updates) stable across a pure value edit, which doesn't change the count.
    - **The cursor/selection stays on the exact split line just edited**, not the parent (Markus) — `handleCellValueChanged` carries the edited line's index through `pendingFocusLineIndexRef`, and the settle effect looks that line's row up by `__parent` + `__lineIndex` rather than reconstructing its (now count-dependent) synthetic id, falling back to the parent's own row if that line no longer exists by the time it runs.
    - **The invariant (parent `amountCents` === Σ `lines[].amountCents`) is enforced on every save**, not checked after the fact: `persistTx` recomputes the *last* line's `amountCents` from `src/lib/split.js`'s `withRemainder()` whenever `lines.length > 1` — every other line is directly stored as typed. The parent's own amount becomes non-editable once split (a fixed total that splitting only ever redistributes, never changes); a single line (≤1) keeps the older "parent is authoritative, mirrors down into the line" behavior instead.
    - Each line has its own `note` (shown "↳ `note`" in the Empfänger column — display-only prefix, the raw editable value has none), its own `amountCents` (editable except on the last/remainder line), `categoryId` (via the same `CategoryEditor` popup as the parent, parameterized per-row through a `cellEditorParams` *function* rather than a static object, and a new `initialCategoryId` prop on `CategoryEditor` replacing its old `data.lines[0]`-derived default so it works identically for a line or a parent), and `tags`.
    - **Known gap, narrowed, not fully closed:** adding/splitting-further is now Ctrl+T, and removing a line is the Delete key (same as a whole transaction) — both fully keyboard-reachable. The expand/collapse chevron itself is still mouse/tap-only; AG Grid's own cell focus (reachable via arrow keys) is a different thing from real DOM focus on the button inside that cell, so Enter on a keyboard-focused split-column cell still doesn't toggle it. Worth a dedicated pass if real usage shows that's still missed.
    - **Every sortable column has a custom `comparator`, not just Datum** — `glueToParent(getValue)` (module-level helper), given a function that computes a column's "parent-level" value, compares *every* row by its own parent's value; a line row and its parent always tie under this rule, and JS's stable sort preserves `displayRows`' own build order for the tie, keeping a transaction's lines adjacent to it regardless of which column is actually driving the sort. Session 27's first pass only wired this up for Datum (the always-active default sort) — Markus then caught that sorting by any *other* column still scattered an expanded transaction's lines back to the top, since only Datum's comparator existed yet. Each column's "parent-level" value is a small extracted function (`kontoValue`, `betragValue`, `kategorieValue`, `unterkategorieValue`, `tagsValue`, plus inline ones for Datum/Empfänger/Details) shared between its `valueGetter` (parent-row display) and its `comparator`, so the two can't quietly drift apart. Betrag glues a line to its parent's *total* for sorting too, not the line's own distinct amount — sorting the whole grid by every individual split line's own amount wouldn't be meaningful either way.
    - Line rows get a distinct subtle background (`--color-line-row-tint`, `src/index.css`) via `getRowStyle`, so they read as visually separate from ordinary transaction rows.
    - The split column's own button clicks (add/remove/split-further) route through the same `pendingFocusIdRef`/`rows`-settle mechanism `handleCellValueChanged` already uses, rather than leaving AG Grid's own selection state to drift — these are button clicks, not cell edits, so without this nothing told the grid which row to re-select once each action's Firestore round-trip landed, which showed up as a stale blue tint on an unrelated row (Markus, screenshot).
    - **A second, independent way to land the cursor back on the grid — `focusGridMidViewport()`, used by the pinned panel's own Ctrl+K/Escape/Enter flow (below) — has its own immediate `setTimeout(0)`, not gated on `rows` settling at all** (it often has nothing to wait for; a filter change doesn't necessarily change the row *set*). Two genuinely independent deferred callbacks that each call both `setFocusedCell` and `setSelected` can interleave — an edit made just before Ctrl+K/arrow/Enter can still have its own settle effect pending when this one fires — and since neither pair of calls is atomic against the other's, one callback's `setFocusedCell` and the other's `setSelected` can each "win" separately, splitting the cell-focus rectangle and the blue selection tint across two different rows (Markus, screenshot). **Note: this race was a plausible reading of Markus's screenshot but turned out *not* to be what he was seeing** — the tint that stayed on "the middle visible row" while scrolling was AG Grid's own row-hover highlight sticking to a touchscreen's parked pointer, now turned off via `suppressRowHoverHighlight`. The guard below is kept anyway as a harmless safeguard against the real (if unobserved) interleaving. Guarded with a shared monotonic counter (`focusClaimRef`) bumped by every claimant (`claimPendingFocus(id, colId, lineIndex)`, used everywhere the old three-line `pendingFocusIdRef`/`pendingFocusColRef`/`pendingFocusLineIndexRef` assignment used to appear directly, plus `focusGridMidViewport`'s own claim) — each deferred callback checks the counter is still its own claim right before applying anything, so a superseded claim silently no-ops instead of partially overwriting a newer one; `focusGridMidViewport` also clears `pendingFocusIdRef` outright so a same-id edit-settle that hasn't fired yet can't spuriously re-claim later off some unrelated future `rows` change.
  - **Konto and Kategorie/Unterkategorie are custom popup editors, keyboard-chainable end to end:**
    - `src/KontoEditor.jsx` — two account pickers (Von/Nach), resolving spec.md §3a's open question of how you edit a merged Konto cell. Both columns (Kategorie and Unterkategorie) open `src/CategoryEditor.jsx`'s cascading Kategorie→Unterkategorie pair, since Kategorie has no stored value of its own.
    - Both use `src/Listbox.jsx` — a custom-rendered dropdown, not a native `<select>`. A native select inside these popups wasn't opening at all on tap on Markus's device (most likely a stacking/rendering conflict between a native form control's own popup and AG Grid's own popup positioning, which applies a CSS transform) — building it ourselves sidesteps that instead of chasing the exact cause. Focusing a Listbox opens it immediately; Enter on a highlighted option commits it and (via each editor's `onEnter` wiring) either advances to the next Listbox or applies and closes, matching the flow Markus described: cursor on the cell → Enter → arrow through the first list → Enter → arrow through the dependent list → Enter → applied, back to normal grid navigation.
    - **`Listbox` gained an opt-in `searchable` prop (Sept 2026, Markus: "i need a quick search bar inside the category selection modal, same as in the tag selection modal")** — a text input at the top of the dropdown, filtering `options` by name as you type (case-insensitive substring match, same rule TagEditor's own search already uses). Off by default (`false`) — `KontoEditor`'s Von/Nach pickers are unaffected; `CategoryEditor`'s Kategorie and Unterkategorie both opt in. When searchable, focusing the Listbox lands keyboard input in the search box instead of the button (deferred to a `useEffect` on `open`, since the box doesn't exist in the DOM at the exact moment `openList()` itself runs) — arrow/Enter/Escape all then operate on the filtered list; Escape clears search text first, same convention as TagEditor's own Escape handling, and only an already-empty search closes the list itself.
    - **Both `groupOptions` and `subcatOptions` dropped their "– wählen –" placeholder entry entirely (Sept 2026, Markus)** — with a searchable list, that placeholder (`id: ''`) sitting first in the unfiltered array meant an opened list highlighted it by default instead of the first *real* match, both for a brand-new/uncategorized row's Kategorie list and for Unterkategorie's own list right after picking a group. (First reported for Kategorie only, with Unterkategorie believed already fine; a follow-up from Markus confirmed the identical problem there too, once actually exercised — same fix applied to both.) Clearing the whole assignment now has its own dedicated action (Delete, below) anyway, so the placeholder wasn't buying anything a real fix couldn't replace.
    - **Picking a Kategorie used to only advance to the open, pre-highlighted Unterkategorie list every *other* time (Sept 2026, Markus: "it does but only after the second time")** — a real stale-closure race, not intermittent: Kategorie's own `onEnter` calls `subcatRef.current?.focus()` synchronously, in the same tick as the `setGroupId` call that just changed it, but React hasn't re-rendered yet — so Unterkategorie's `Listbox.focus()`/`openList()` still runs against the *previous* render's `disabled={!groupId}` (still `true` for the old value) and stale `subcatOptions`, and silently no-ops (a disabled button also can't receive real DOM focus). Re-opening and picking again worked because by then the first pick's state update had already landed. Fixed with a `groupId`-keyed `useEffect` in `CategoryEditor.jsx` instead of relying on the synchronous `onEnter` call for this specific case — guaranteed to run only after React has committed the new render, so `disabled`/`subcatOptions` are always fresh by the time it fires. A `prevGroupIdRef` (initialized to the *current* groupId, not reset every render) distinguishes "groupId actually just changed via a pick" from "this is just the value the editor mounted with," so opening an already-categorized row doesn't spuriously auto-advance before the user has touched anything. Kategorie's own `onEnter` is left in place for the case that already worked — re-confirming the *same* group unchanged via Enter (arrowing through an already-categorized row) — since nothing is stale there and the new effect doesn't fire a second time when `groupId` didn't actually change (no double-open either way).
    - **Delete clears the whole category assignment and closes (Sept 2026, Markus: "i need to be able to delete a category setting")** — `CategoryEditor`'s capture-phase key handler (same one Escape already used) now also handles `'Delete'`, calling a new `onClear()` prop (`clearCategoryDirect`/`clearCategoryToLine` in `Konten.jsx`, both new — nulling `categoryId` directly, bypassing `applyCategoryDirect`/`applyCategoryToLine`'s own truthiness guard, which exists specifically to refuse an *accidental* empty commit from the normal apply path and shouldn't be weakened for a deliberate clear). Guarded against breaking normal text editing in a searchable Listbox's own search box: intercepted only when `e.target` isn't an `<input>` with actual text in it (`e.target.tagName === 'INPUT' && e.target.value !== ''` skips the intercept) — a plain DOM check on the live input value, since `CategoryEditor` has no access to `Listbox`'s own internal search-text state. Since the search box auto-focuses empty the instant the popup opens, "Enter opens the cell, DEL clears and closes" works immediately with no extra keystrokes, while actually typing a search query still deletes characters normally.
    - **Applying always lands the grid cursor on the Unterkategorie cell, regardless of which of the two cells the editor was opened from (Sept 2026, Markus: "when setting a category and untercat combination, upon closing the modal, please move the cursor onto the unterkategory field")** — both columns now have an explicit `colId` (`'kategorie'`/`'unterkategorie'`, previously auto-generated) so `apply()` can target the second one reliably. Reads `api.getFocusedCell()?.rowIndex` *before* `api.stopEditing(true)` (while the cell actually being edited, whichever one it is, still reports as the grid's own focused cell), then calls `api.setFocusedCell(rowIndex, 'unterkategorie')` *after* — winning over whatever `stopEditing` itself would otherwise have restored focus to (the originally-opened cell). No separate "is this really a combination" check needed: every successful `apply()` already is one by construction, since `categoryId` can only ever hold a real Unterkategorie leaf id (§2.6 — Kategorie has no stored value of its own). Scoped to `apply()` only — Abbrechen/Escape and the DEL-clear path above are untouched, since Markus's request was specifically about the "setting a combination" case.
    - Übernehmen/Abbrechen buttons remain for mouse/touch use — the Enter chain is the keyboard equivalent of the same `apply()` call, not a separate path. `apply()` takes an optional override argument for whichever value was *just* committed via Enter, since a Listbox's `onEnter` fires synchronously before React re-renders the state `onChange` just set — reading plain state at that exact instant would still see the previous value.
    - Übernehmen writes directly via `cellEditorParams.onApply` (`applyKontoDirect`/`applyCategoryDirect` in `Konten.jsx`, persisting via the shared `persistTx`) and closes with `api.stopEditing(true)` (cancel — already persisted), rather than going through AG Grid's own `getValue()`/`stopEditing()` commit pipeline. That pipeline's own popup-dismiss listener only ever attaches when the popup is `modal` (`stopEditingWhenCellsLoseFocus`, which this grid never sets) — confirmed never the actual mechanism at play, and confirmed unreliable for a different, still-unidentified reason, so nothing here depends on it working.
    - Betrag's `valueSetter` uses the same "reference account" its `valueGetter` already displays, regardless of the account filter.
    - **Betrag's `cellClass` turns a negative value amber while a filter (account or tag) is active** (Markus, Sept 2026 — asked for red, but spec.md §1b.4 reserves red exclusively for alerts, confirmed with Markus to keep that rule and reuse `--color-expense` instead of carving out an exception). `cellClass` is a function of `p.value` (the same signed amount `valueGetter` already resolved) plus the closed-over `accountFilter`, so it piggybacks on the *existing* `refreshCells({ force: true })` effect keyed on `accountFilter` (right above, in `Konten.jsx`) — the same fix already needed for Betrag's own `valueGetter` to re-run per row when the filter changes, since AG Grid's row-data-keyed change detection doesn't otherwise notice an external closure value changing. No separate re-render wiring needed for the color.
    - **`KontoEditor`'s simplified single-dropdown mode (Markus)**: while an account filter is active (`filteredAccountId`, passed through as a `cellEditorParams` prop from `Konten.jsx`'s own Konto column), every visible row already has the filtered account on one side — `rows`' own filter predicate guarantees it, and `addRow` pre-fills it on a brand-new row. `fixedSide` (`'from'` | `'to'` | `null`, derived from whether `data.fromAccountId`/`toAccountId` equals `filteredAccountId`) picks which side renders as a plain non-editable label ("Von (Abfluss): *Außenstände* (Filter)") versus a `Listbox`; the remaining single field's `onEnter` applies and closes directly instead of chaining, same "single field, Enter closes" convention `TagEditor`'s own redesign established. Falls back to the ordinary two-picker layout (`fixedSide === null`) whenever neither side happens to match — the general case with no filter active, and a real fallback if a filtered row's data somehow doesn't match either side. `apply()` takes an optional `{fromId, toId}` overrides object (rather than one positional arg) so either side's Enter-commit can pass the value it just committed without waiting for a re-render.
    - **Escape cancels outright in both `KontoEditor` and `CategoryEditor` (Markus)** — a capture-phase `window` keydown listener (`addEventListener('keydown', handler, true)`), not a plain React `onKeyDown`. Needed because two other native listeners are also in the running for the same keypress: AG Grid's own `PopupEditorWrapper`, and — when a nested `Listbox` inside the editor has its own dropdown list open — that `Listbox`'s own native listener, which claims Escape *only while its list is open* (closing just that list, one layer at a time; see `Listbox.jsx`, which has no top-level Escape handling of its own when its list is already closed). Capture phase runs top-down before any of those bubble/target-phase listeners, so one Escape press reliably cancels the whole popup with `api.stopEditing(true)` regardless of which nested field currently has an open list — verified via a disposable Playwright harness (mounting `KontoEditor` directly against a minimal grid, standard practice this project for AG-Grid-popup race conditions): confirmed for full mode, both simplified-mode fixed-side cases, and specifically the nested-list-open case.
    - A parent row's Kategorie/Unterkategorie stays editable only while it has ≤1 line — once split, per-line category editing happens on the expanded line rows instead (above).
  - **Tags — the real mechanism (spec.md §2.5), not the earlier comma-separated-text placeholder.** New `tags` Firestore collection, loaded the same way `accounts`/`categories` already were (`onSnapshot`, `tagById` lookup memo). `src/TagEditor.jsx` is a new popup cell editor, same family as `KontoEditor`/`CategoryEditor` — a multi-select autocomplete: type to filter existing (non-archived) tags, Enter/click a suggestion to add it as a removable chip, "Tag '…' erstellen" when nothing matches. Same direct-write-then-`stopEditing(true)` pattern as Konto/Category's own Übernehmen (`applyTagsDirect`/`applyTagsToLine` in `Konten.jsx`), with a fallback `valueSetter` for however else an edit might end (blur) — unlike Kategorie's, this one has no truthiness guard, since an empty tag list is a fully valid commit. `cellEditorParams` is a per-row function exactly like Kategorie/Unterkategorie's, branching on `__isLine` the same way.
    - **claim-category sequencing (spec.md §2.5) is enforced in the suggestion list itself**: a `groupingType: "claim-category"` tag (Meal/Taxi/…) is filtered out of the autocomplete entirely unless the line already carries a tag with `groupingType: "claim"` — not a separate mode, just a filter condition alongside "not archived" and "not already selected."
    - **New tags are always `class: "grouping"`, never `allocation`** (spec: allocation tags are fixed/pre-seeded, a deliberate Settings-area action, never created while entering a transaction) — `createTag()` in `Konten.jsx` writes a new doc with a slugified id (collision-suffixed if needed) and `groupingType: null`.
    - **`createTag()` parses a colon as parent:child** (Markus, real-usage feedback, Sept 25) — "Schottland:Fähre" reuses an existing top-level "Schottland" tag by name (case-insensitive) or creates one, then creates/returns a real child tag under it (`parentTag` set, spec.md §2.5's hierarchy). The id actually applied to the line is always the child's, matching spec's own breakdown-rollup note that an untagged child contributes nothing to its parent's rollup. `src/lib/tagStyle.js`'s new `qualifiedTagName(tag, tagById)` renders a child as "Schottland: Fähre" everywhere a tag name is shown (TagEditor's chips/suggestions, the Tags column, the sort comparator) — a bare "Fähre" would be ambiguous outside Verlauf's own already-grouped breakdown block (unlike that block, §2.7's own display-convenience note doesn't apply here).
    - **Suggestions are usage-derived for grouping tags** (Markus, real-usage feedback, Sept 25 — the original build offered every seeded/created grouping tag regardless of whether it was ever actually used, and separately never offered a *pre-mechanism free-text* tag like an informal loan's own label at all, since those were never real tag documents to begin with). `usedTagValues` (`Konten.jsx`, a `Set` built from every value in every line's `tags[]` across all loaded transactions, any year) drives two things in `TagEditor`: a grouping tag is only ever suggested if it's currently used somewhere, **whether browsing or typing** (revised same day — an earlier pass only applied this filter to the empty-input browse case, which Markus caught as still cluttered while searching); and any value in `usedTagValues` that isn't a real tag id at all becomes a selectable "legacy" suggestion in its own right (not just a chip once it's already on the current line) — reusing Markus's existing informal-loan/trip labels without silently forking near-duplicates. The one exception to the usage gate: a real tag whose *qualified name* exactly matches the typed text is still offered even if unused, so a real-but-currently-unused tag never becomes a true dead end (can't create a duplicate of it, and otherwise couldn't reach it at all). Allocation tags are exempt from the usage filter entirely (always structurally relevant). `canCreate`'s "does this already exist" check covers both real tags and legacy values, so typing an exact existing legacy string's name no longer also offers a redundant "create" option next to the real match (a bug caught by the harness re-run below).
    - **Tag creation offers a type** (Markus asked directly: "how can i create a new travel tag or statement tag etc?" — resolves the open question logged the previous session). Instead of one "Tag '…' erstellen" row, up to five — one per groupingType (Reise/Projekt, Abrechnung, Anspruch, Anspruchsart — the last gated by the same claim-tag-already-present sequencing rule as everywhere else — plus Unbestimmt) — each independently navigable/selectable via the same Arrow/Enter mechanism as ordinary suggestions. `CREATE_TYPES` (`TagEditor.jsx`) lists Unbestimmt *first* deliberately: a plain Enter with nothing arrowed to still creates unspecified, exactly the old default behavior, so this is additive, not a workflow change for anyone not using it. `onCreateTag`/`createTag`/`createPlainTag` (`Konten.jsx`) all now take the chosen `groupingType` through to the actual `setDoc`; for a "Schottland:Fähre"-style parent:child creation, the chosen type applies to the child (the one being categorized in context), a newly-implied parent still always starts unspecified.
    - **Selecting a suggestion (or a create-type row) applies and closes the popup immediately** (Markus: "rarely ever will i select two tags in one go") — `selectSuggestion()` computes the new tag id directly and calls `onApply`/`api.stopEditing(true)` itself rather than routing through `setSelectedIds` first (that state update wouldn't be visible on the same call anyway, and the popup is closing regardless). Removing a chip via its own `×` deliberately still doesn't auto-close — only *adding* a tag does — so fixing a mis-tagged line (remove the wrong one, pick the right one) stays a single visit to the popup.
    - **Enter always picks whatever's currently highlighted, including the default first suggestion on a totally untouched, just-opened popup** (fixed same day — Markus: Enter on "a selected tag" wasn't applying it). The popup opens with `highlight` already at index 0, so a suggestion is visibly highlighted before the user does anything at all — the Enter handler previously special-cased "empty input" as "just close, ignore whatever's highlighted," which silently contradicted what was on screen. Now: `optionCount > 0` always wins (`selectSuggestion(highlight)`), and only truly nothing-to-select falls back to closing with whatever's already applied.
    - **The 5 most-recently-used tags sit above a separator at the top of the empty-input browse list** (Markus). `recentTagValues` (`Konten.jsx`, a plain array — every used value, real tag id or legacy string alike) — `TagEditor` takes the first 5 of these that are still real suggestion candidates (already filtered for archived/selected/claim-category-sequencing) and lists them first, with an `<hr>` after, only while the input is empty (typing a search drops back to plain relevance-ordered matches — a "recent" section didn't make sense mixed into active search results). **Ordered by each transaction's own `updatedAt` (fixed same day — Markus: tagging an old row didn't bring that tag to the top), not by the transaction's booking `date`.** `persistTx` now sets `updatedAt: Date.now()` on every save, distinct from `createdAt` (§2.6, set once and never again). Known imprecision, not chased further: `updatedAt` is per-*transaction*, not per-tag-application, so editing any other field on a transaction also bumps every tag already sitting on it — close enough for "what did I just tag," not worth a per-tag timestamp.
  - **The grid itself is now filterable by tag, two ways (Markus's own proposal, picked "B and C" from three options offered):**
    - **B — click any tag chip anywhere in the grid to filter by it.** The Tags column's `cellRenderer` chips are real `<button>`s — a ref callback assigns a genuine native `onclick` directly on each button (`el.onclick = ...`), not a React `onClick` prop, calling `setAccountFilter(id)` (the same generalized filter state the pinned panel's tag rows already use, so this works for *any* tag — allocation or grouping — with no new filter concept).
      - **A child tag's chip splits into two independently clickable halves** (Sept 2026, Markus: "I need to be able to filter for the parent tag, too, and show the total: Schottland in Schottland:Whatever"). When `tagParent(t, tagById)` (`tagStyle.js`, factored out of `qualifiedTagName()` so both agree on what "has a parent" means) returns a real parent tag, the chip renders as a `<span>` pill containing two real `<button>`s — "Schottland" (`setAccountFilter(parent.id)`) and "Fähre" (`setAccountFilter(id)`, unchanged from before) — each with its own native-`onclick` ref callback, same race-condition fix as the single-button case. A plain tag with no parent still renders as one button, unchanged. Filtering by the parent rolls up every direct child (and any line tagged with the bare parent itself) via `tagFilterMatchIds()` (below) — used identically by `rows`' own filter predicate and by `tagFilterTotal()`'s sum, so the grid and the toolbar total can't disagree.
      - **Why a native listener, not React's `onClick` + `stopPropagation()` (which was the first attempt, and didn't work — Markus caught it opening `TagEditor` at the same time):** the exact same race this codebase has hit twice before (`Listbox.jsx`'s Enter fix; the Ctrl+Enter/AG Grid capture-phase fix, both Konten.jsx). AG Grid's `singleClickEdit` starts editing via a plain native click listener attached on the cell/row itself — a real DOM ancestor of the chip button. React 17+ doesn't attach a real listener on the button at all; it attaches *one* delegated listener way up at the app's root container and only dispatches to a component's `onClick` once the raw event has already bubbled *past* every ancestor in between, AG Grid's own cell listener included — so AG Grid's closer, real listener always won the race and started editing before a React `onClick`'s `stopPropagation()` ever got a chance to run. A listener assigned directly on the target element itself runs at the true target phase, strictly before the event starts bubbling anywhere, so it wins outright instead of trying to out-run an ancestor. **Verified against a real AG Grid instance** (scratchpad harness, an editable column with a dummy popup editor whose mere presence in the DOM proves whether editing started) — confirmed clicking a chip filters *without* opening the editor, and clicking elsewhere in the same cell still does open it.
    - **C — a real header filter on the Tags column, same convention every other column will eventually get.** Caught a real, previously-unnoticed gap while building this: **no column in the grid had `filter` set at all** — spec.md §3a's "every column independently filterable... via AG Grid's native column filters" was never actually wired up anywhere, not just for Tags. `filter: 'agTextColumnFilter'` (a plain "contains" text search against the resolved qualified names, via a new `filterValueGetter` — not a tag-picker popup, the simplest thing that works without a custom filter component) — **verified this is genuinely Community-tier, not gated behind Enterprise**, by building an isolated AG Grid instance registering only `AllCommunityModule` (same as the real app) and confirming the filter icon, popup, and live filtering all work with zero Enterprise console warnings (screenshot-checked, same discipline as Session 25's dark-mode verification and the earlier Range-Selection/fill-handle check the same session that DID turn out Enterprise-gated — `TextFilterModule` is a different, Community module from `CellSelectionModule`).
    - **Extended the same day to every other column except Konto** (Markus: "add filter buttons to the other columns, too") — Datum, Empfänger, Kategorie, Unterkategorie, and Details all get `filter: 'agTextColumnFilter'` (Datum's stored `"YYYY-MM-DD"` string compares/sorts fine as plain text, no real date-typed filter needed); Betrag gets `filter: 'agNumberColumnFilter'` with its own `filterValueGetter` dividing the stored cents by 100, so typing "100" means 100€ — what's actually displayed — not the raw stored integer. **Konto is the one deliberate exception, not an oversight**: spec.md §3a already settled that Konto's displayed value is *derived*/relative to whichever account is filtered, so a plain column filter on it was explicitly rejected in favor of the dedicated Konto dropdown + pinned panel — same reasoning still holds here.
    - **Clicking a tag (chip or panel) also sets the Tags column's own header filter to match** (Markus) — one `useEffect` keyed on `accountFilter`/`filteredAccountId`/`tagById` (not duplicated at each click site) calls `api.setColumnFilterModel('tags', {filterType: 'text', type: 'contains', filter: <qualified name>})` whenever the filter is a tag, and clears it (`null`) whenever it isn't — `setColumnFilterModel` is async, so the effect awaits it before calling `api.onFilterChanged()` (per the method's own doc comment). **`'contains'`, not `'equals'`** (changed alongside the parent-tag filter above) — a parent filter's label ("Schottland") needs to match a row's own text ("Schottland: Fähre"), not equal the whole cell; `rows` itself is what actually decides inclusion, this only keeps the header filter box/icon visibly consistent with it.
    - **A "Filter zurücksetzen" button** (top toolbar, right-aligned via `ml-auto`, next to "+ Neue Buchung") appears only when something is actually active — `accountFilter` truthy, or AG Grid's own `isAnyFilterPresent()` (mirrored into React state via the grid's `onFilterChanged` prop, since AG Grid's internal filter state isn't otherwise visible to React). Clears both at once: `setAccountFilter(null)` plus `api.setFilterModel(null)` (every column's filter, not just Tags').
    - **Color-coding** (`src/lib/tagStyle.js`'s `tagColorVar(tag)`) maps class/groupingType to the CSS tokens added to `index.css` for this — allocation reuses `--color-savings` (already purple, §1b.4), the four grouping types get their own new tokens (`--color-tag-project` teal, `--color-tag-statement` slate, `--color-tag-claim` indigo, `--color-tag-claim-category` rose). **Revised (Markus, real-usage feedback, Sept 25): `groupingType: null` and an unresolved legacy string now both return `null` (no color) rather than the statement-gray token** — a freshly-created "unspecified" tag rendering as a solid gray fill looked inconsistent right next to an old free-text tag's plain outline, so both now get the exact same plain dashed-border/no-fill treatment; only a tag with a genuinely determined type gets a colored fill. Chips render via `color-mix(in srgb, var(...) 15%, transparent)` for the background, the plain var for the text, when a color exists — same helper used by TagEditor's own chips, the Tags column's read-only `cellRenderer`, and now the pinned panel (below).
    - **Backward compatibility with pre-mechanism free-text tags is deliberately conservative, not auto-migrating:** every tag typed before Sept 24 is a raw string with no real tag document behind it. `tagById` lookups fall through to the plain string everywhere, styled per the point above — they're not silently promoted into new tag documents (would risk quietly multiplying the `tags` collection with typo-derived near-duplicates unsupervised), but Sept 25's usage-derived suggestions (above) do make them properly *reusable* now, which was the actual gap Markus hit in practice.
    - **`slugify()` (`TagEditor.jsx`) transliterates German umlauts/ß before stripping other non-alphanumeric characters** — caught by the harness re-run below: "Fähre" produced the id `f-hre` before this (the diaeresis just vanished), now `faehre`.
    - **Verified via a standalone Playwright harness** (scratchpad, not part of the repo — same approach as Session 25's AG Grid dark-mode check), re-run for every fix in this pass, not just the original build: usage-derived suggestions (an unused tag hidden on empty browse, reachable by typing its name), a legacy free-text value surfacing as a real suggestion, the `canCreate` fix, the slugify fix, and "Schottland:Fähre" correctly reusing an existing parent and creating a real child — checked against real rendered/interactive output with fake data before Markus's own test, not just reasoned from source.
  - **Allocation-tag balances surface in the pinned panel itself, as full filter buttons — not a read-only display** (`src/lib/tagBalance.js`'s `tagBalance()`/`tagJahresende()`, tested in `tagBalance.test.js` — same shape and same "no Firestore access, pure function" discipline as `balance.js`), per spec.md §3a's own "Live consistency checks... should surface here" note and Markus's explicit request (Sept 25) to actually see them, not just trust the invariant holds invisibly. Summed per *line* (a tag lives on `lines[]`, §2.6), directional sign generalizing `balance()`'s own account-position rule to a tag's full `reconciliationTargetAccountIds` set: `+` when the parent transaction's `toAccountId` is one of the tag's targets, `−` when `fromAccountId` is, both checked independently (not either/or) so a transfer *between* two of a tag's own target accounts (e.g. Aktien → Crypto, both inside Anlage Familie's combined pool) nets to zero rather than double-counting. **Uses the line's own signed `amountCents` directly, not `Math.abs()` first** (fixed same day — Markus: Anlage Familie off by a real amount) — unlike `balance()`'s account-position rule, where a transfer's `amountCents` is always already a stored positive magnitude (§2.6) so `Math.abs()` is a no-op there, a *line's* own sign can genuinely differ from its parent's (§2.6's salary-split example: positive Gehalt / negative Steuern), and forcing `Math.abs()` first silently flipped a negative line's real contribution to positive. Exactly mirrors `migration/transform-transactions.py`'s own `allocation_tag_delta()` — the reference implementation already checked against the real Gsheet totals during the original migration (`verify_tag_sums()`), which never took `abs()` either, for the same reason. `panel`'s `useMemo` (`Konten.jsx`) computes each reportingGroup's `tagItems` — every allocation tag whose targets intersect that group's own account ids — appended into the **same** account `<ul>` as real filter buttons, not a separate read-only list (revised same day: Markus explicitly wanted them "fully part of the cycling with arrow buttons," not just visible). A tag targeting a combined multi-account pool (Anlage Familie/Sophia) shows once under that group, not once per account.
  - **Amazon Julia (DE)/(FR), Amazon Markus (DE)/(FR), and Geld verliehen/geliehen collapsed into one shared `aussenstaende` account** (design conversation with Markus first, Sept 2026 — spec.md §2.2/§3a; CPAM and Reisekosten Airbus deliberately kept separate, each slated for its own dedicated tracker later). Which specific claim/loan a transaction belongs to is now carried entirely by a `claim`-type grouping tag, not by which of five near-identical accounts it happened to sit on. The four Amazon accounts and `geld-verliehen-geliehen` stay defined in `migration/seed/accounts.json` (now `tracked: false`, the same pattern already used for `giro-sophia` — not deleted, since already-imported live data may still reference them until the historical migration below actually happens) with a new `aussenstaende` entry added.
    - **New `src/lib/tagBalance.js` function, `tagFilterTotal(tagId, asOfDate, transactions, aussenstaendeAccountId, tags)`, tested (`tagBalance.test.js`, 5 new cases).** (Sept 2026 follow-up: gained the trailing `tags` parameter for parent-tag rollup — see `tagFilterMatchIds()` in the shared-functions list below.) Distinct from `tagBalance()` above (which needs a tag's own stored `reconciliationTargetAccountIds` — the right tool for an allocation tag with real, possibly multi-account targets) — a claim tag has no such per-tag target to store, since every one of them shares the exact same single `Außenstände` account, hardcoded as a parameter rather than configured per tag. **Resolved directly with Markus, a real design conversation before any code:** a tagged line contributes the *position*-based sign (§2.8's account-position rule, generalized) when its parent transaction touches Außenstände — this was the actual point of the conversation, since a plain signed-sum of the line's own `amountCents` would always total to zero for a claim tag by construction (every line touching it is, definitionally, one leg of a transfer). A genuinely single-sided line (a real expense/income, not touching Außenstände) contributes its own natural sign directly — the common case for a trip/expense tag. A transfer between two *other* real accounts is excluded from the total entirely.
    - **The panel's Außenstände group gets its `tagItems` from a different source than the allocation-tag groups do** (`panel`'s `useMemo`, `Konten.jsx`) — since claim tags have no stored target to filter by, the candidate list is *discovered*: every distinct tag used on a line whose transaction touches the `aussenstaende` account id, filtered to those with a non-zero `tagFilterTotal` ("open" — a settled claim's total nets to zero and silently drops off the list, same mechanism spec.md §3a already describes for loans). Rendered through the exact same merged `<ul>`/button/color machinery the allocation-tag rows already use — no new rendering path needed.
    - **Toolbar "Angezeigt:" readout** (renamed from "Summe „…":" Sept 2026, Markus — dropped the tag-name interpolation entirely, since it's redundant with whatever's already visibly selected) shows the total for whatever tag is currently filtered — branches on the tag's own `class`: an allocation tag reuses `tagJahresende` (the real, correct multi-target figure, same as its own panel row); anything else (grouping tag, or an unresolved legacy string) goes through `tagFilterTotal`. Never shown for an account filter — the panel already shows an account's own balance.
    - **Every right-aligned toolbar element shares exactly one `ml-auto` wrapper (fixed Sept 2026, Markus — screenshot showed a large empty gap between "Filter zurücksetzen" and the (i) icon)** — a real flexbox bug, not a stray style: the Summe/Filter-zurücksetzen block and the (i) icon each had their *own* `ml-auto`, and CSS flexbox splits a row's free space *evenly across every auto-margin item present* rather than collapsing them together, so two auto-margin siblings end up with visible space between them, not hugging the right edge as one group. Fixed by always rendering one shared wrapper (`Angezeigt:`/`Filter zurücksetzen` conditionally inside it, the (i) icon unconditionally as its last child) with a single `ml-auto` and its own internal `gap-3` — the (i) icon still lands at the far right even when nothing else in the wrapper is rendered.
    - **Migration completed (Sept 2026):** Markus created the `aussenstaende` account and reassigned every historical Amazon/loan transaction onto it directly in Firestore (no live access from this session either way — this was entirely his own action). The five superseded accounts (`amazon-julia-de`/`-fr`, `amazon-markus-de`/`-fr`, `geld-verliehen-geliehen`) are removed from `migration/seed/accounts.json` outright, not just marked `tracked: false` — leaving them in, even inert, risked a future `ImportScreen` re-run silently recreating them.
    - **`accountFilter` (the shared state, despite its name) now holds either a real account id or an allocation tag id.** `filteredAccountId` (a derived value, `accountById[accountFilter] ? accountFilter : null`) is what every *account-specific* consumer reads instead when it only makes sense for a single reference account — Konto's re-signed arrow display, Betrag's re-signed amount, `addRow`'s account prefill, the plain `<select>` dropdown's `value` (accounts-only, so a tag filter shows it as "Alle Konten" rather than nothing selected). It's deliberately `null` whenever the filter is actually a tag: a tag can span several target accounts at once (Anlage Familie), so there's no single meaningful "the other side" to re-sign against — a tag-filtered row just shows its own natural, unfiltered Konto arrow and amount sign. `rows`' own filter predicate, by contrast, reads `accountFilter` directly and branches on `filteredAccountId` itself to decide *how* to match (by `fromAccountId`/`toAccountId` for an account, by `lines[].tags` containment for a tag).
    - **The account and tag buttons share one `<ul>` per reportingGroup, tag rows last**, so the existing keyboard cycling (`closest('ul').querySelectorAll('button')` for Up/Down) sweeps through both together for free — no separate handling needed. The shared DOM marker was renamed `data-filter-id` (was `data-account-id`), read by both the keyboard handler and Ctrl+K's own "return to the active filter's button" lookup. A tag button's color comes from `tagColorVar` (a plain `style`, since Tailwind can't do arbitrary dynamic CSS-var text colors); active-state highlighting is `font-semibold` for a tag (keeping its own color) vs. the existing computed-blue swap for an account.
  - **Datum is a plain text cell, not a picker of any kind** (Markus specifically didn't want one) — `parseFlexibleDate()` accepts the stored `"YYYY-MM-DD"` as-is, or European shorthand (day first, always): `D.M`/`D/M` with the year taken from the app's own year selector, or `D.M.YYYY`/`D/M/YYYY` with an explicit year (2-digit read as 20XX). Goes through the same default AG Grid text editor + `valueSetter` pipeline `displayLabel`/`detail` already use successfully — no custom editor component, since the popup-based approach tried first for this had its own reliability problems (see above) that a plain text field doesn't have.
  - **Columns are fixed** — `defaultColDef={{ suppressMovable: true }}` blocks drag-reordering; Community edition never shipped a hide-columns tool panel (an Enterprise module, never registered).
  - **`singleClickEdit={true}`** on the grid overall — AG Grid's default is double-click-to-edit.
  - **Row selection (the blue tint) follows keyboard cell focus, not just clicks** (`onCellFocused` calls `setSelected` on whichever row the focus lands on) — also means "+ Neue Buchung"'s insert-below-selected-row follows wherever the keyboard cursor is. **This same handler also invalidates a stale focus claim (Sept 2026 — see the Tab-focus-jump-back fix below).**
  - **Tab-after-editing no longer jumps focus back to the just-edited cell (Sept 2026, Markus: hitting Tab "moves briefly over... then jumps back to the edited cell," needing a second Tab press).** Root cause: every plain-inline-editable cell's commit (`handleCellValueChanged`) unconditionally calls `claimPendingFocus(tx.id, colId, ...)`, and the `[rows]`-keyed settle effect (above) re-applies `setFocusedCell(row, colId)` for that exact claim once the async Firestore round-trip lands — but AG Grid's own Tab-to-next-cell had, by then, already moved focus *past* that same claim, which the settle effect had no way to know about. Fixed inside `onCellFocused` itself: whenever the cell that focus actually just landed on (identified the same way the settle effect identifies its own target — transaction id + colId + line index) does **not** match the still-outstanding claim, that claim is invalidated on the spot (`pendingFocusIdRef.current = null`, plus bumping the shared `focusClaimRef` counter so even an in-flight `setTimeout` bails via its existing staleness check). A real Tab/arrow/click always fails this match and invalidates; the settle effect's *own* later `setFocusedCell` call is the one case that's guaranteed to match, so it never self-invalidates. Doesn't affect Enter (stays on the same cell by default, matches the claim, refocus is a harmless no-op) or the custom-popup columns (Konto/Kategorie/Tags apply via `persistTx` directly and `stopEditing(true)`, which never fires `onCellValueChanged`/this claim at all).
  - **Global shortcuts:** Ctrl/Cmd+`'+'` triggers "+ Neue Buchung" (noted: also the browser's own zoom-in shortcut everywhere, so it may not be fully reliable on every platform). **Ctrl/Cmd+N removed again (Sept 2026, Markus: "'+' is enough")** — briefly existed alongside `'+'`, cut since it carried the same real browser-reservation risk ("new window," nearly everywhere) without being worth keeping once asked. Ctrl/Cmd+K always jumps into the pinned panel's account boxes (never the plain `<select>`) — the active filter's own button if one is set, otherwise the first account overall. Ctrl/Cmd+T ("Teilen") starts/continues a split on whichever transaction the cursor is on (the split column's own ✚, above). **Ctrl/Cmd+H** (Sept 2026, Markus) toggles "Kürzlich gelöscht" — flagged to Markus as a real, untested risk: Ctrl+H is Chrome's own reserved "Show History" shortcut on Windows/Linux (Cmd+H is macOS's "Hide window"), the same class of browser-chrome-level reservation already accepted for Ctrl+T; may behave differently in an installed PWA's standalone window (no History UI to reserve it for) versus an ordinary tab, genuinely unconfirmed either way without testing on Markus's own devices. **Ctrl/Cmd+I** (Sept 2026, Markus) toggles the keyboard-shortcuts help popover (below) — chosen specifically because, unlike H/T, it has no known browser-reserved meaning. **Ctrl/Cmd+F and Ctrl/Cmd+S** (Sept 2026, Markus: "ctrl+f opens the filter modal for the active column... ctrl+s sorts the active column (toggle asc and desc)") — both act on whichever column currently holds the cell-focus rectangle (`api.getFocusedCell()?.column`), guarded by that column's own `isFilterAllowed()`/`isSortable()` so a non-filterable/non-sortable column (Split, the delete column) safely no-ops. Ctrl+F calls `api.showColumnFilter(colId)` after snapshotting the column's existing `getColumnFilterModel`; while that "filter session" is open (tracked as a plain closure variable in the one effect, not React state), Enter calls `hideColumnFilter()` and keeps whatever was typed (AG Grid's own text/number filters already apply live as you type, on their own ~500ms+ debounce — nothing else to "apply"), while Escape restores the snapshotted model via `setColumnFilterModel(...).then(() => api.onFilterChanged())` before closing. Ctrl+S reads `column.getSort()` and toggles strictly between `'asc'`/`'desc'` via `api.applyColumnState({ state: [{ colId, sort: nextSort }], defaultState: { sort: null } })` — deliberately never back to unsorted, matching Markus's literal wording. Both return focus afterward to the currently *selected* node (`api.getSelectedNodes()[0]`), not a snapshotted row index, since Enter's own filter can hide the row it started from — a `returnFocusTo` helper falls back to the new first displayed row when that happens, so the cursor never gets stranded with no focus at all. **Verified via a disposable Playwright harness** against a mocked `firebase/firestore` (seeded accounts/categories/transactions): asc→desc→asc toggling confirmed via each header's own `aria-sort` attribute (not naive DOM cell order, which doesn't reliably reflect AG Grid's virtualized row order — read cells by their own `row-index` ancestor instead); the filter's Enter/Escape behavior confirmed both when the previously-selected row survives the new filter (lands back on it exactly) and when it doesn't (falls back to the new first row), plus the delete column's guard confirmed inert for both keys.
  - **Tab on the Tags column adds a new row instead of AG Grid's own default Tab-to-next-cell navigation (Sept 2026, Markus: "when i hit tab on the tags field or while within the tags drop down, i want a new empty row to be created below the current one")** — two separate handlers cover the two ways this can happen, since only one of them has access to what TagEditor itself knows:
    - **Not currently editing** (the Tags cell is merely focused): a capture-phase `window` listener checks `api.getFocusedCell()?.column.getColId() === 'tags'` and `api.getEditingCells().length === 0`, then calls `addRow()` directly — same capture-phase-beats-AG-Grid's-own-core-navigation reasoning as the Ctrl+T listener right below it (Tab is one of the keys AG Grid's own keyboard service claims natively for cell-to-cell movement, so a bubble-phase listener would lose that race).
    - **Mid-edit, the TagEditor popup open**: handled inside `TagEditor.jsx`'s own existing native keydown listener instead (a new `Tab` branch alongside its Enter/Escape/Backspace ones) — needs to be there, not centralized in `Konten.jsx`, because it has to `apply()` (commit whatever chips are already added) *before* handing off to a new `onTabAddRow` prop (wired to `addRow` from both of the Tags column's `cellEditorParams` branches, parent and line row alike) — the grid-level listener has no way to reach TagEditor's own in-progress `selectedIds` state to do that safely itself. Deliberately doesn't also apply whatever's still typed but unconfirmed in the search box — same as Enter's own fallback behavior, typing something without pressing Enter to add it as a chip first was never a commit path either way.
    - Verified via a disposable Playwright harness: Tab on a focused-but-not-editing Tags cell adds a row; Tab from inside the open dropdown, after picking a tag, both applies that tag *and* adds a row, with the popup closing cleanly either way.
  - **Keyboard-shortcuts help popover** (Sept 2026, Markus: "create a small (i) icon... shows the keyboard shortcuts... when i hover over it") — a small circular "i" button. One shared `shortcutsOpen` boolean drives it from three independent inputs: `onMouseEnter`/`onMouseLeave` (plain hover), Ctrl+I (toggles the same state so it works without the mouse), and Escape while open (a capture-phase listener with a conditional `stopPropagation()`, so it reliably wins over whatever else might otherwise claim Escape — an open cell editor's own cancel, the armed-delete discharge — rather than silently losing that race). Content is a plain hardcoded list, kept in sync by hand with whatever shortcuts actually exist above — not derived from a single source of truth, since there wasn't already one to derive from. **Moved to the very last/rightmost item in the toolbar (Markus, Sept 2026 — originally the first item)** — its own `ml-auto` pushes it there even when the Summe/Filter-zurücksetzen block before it isn't rendered at all; the popover itself opens `right`-anchored now, not `left`, so it doesn't overflow off-screen from a far-right trigger. **Content simplified the same day (Markus: "remove the additional explanations in brackets. remove entf, esc, tab / pfeiltasten")** — dropped the Entf/Escape/Tab-Pfeiltasten rows entirely and every parenthetical elaboration on the remaining ones, down to just the five actual global shortcuts above, each one line.
  - **The pinned panel's account buttons have their own roving keyboard navigation, richer than the `<select>` above:** ArrowUp/Down moves within one reportingGroup's `<ul>` and applies the filter live as it moves (`data-account-id` on each button, `data-group` on each panel `<div>`); ArrowLeft/Right jumps to the first account of the adjacent group box in fixed order (Barkonten/Sparkonten/Geldanlage/Außenstände) via DOM sibling traversal. Escape resets to "Alle Konten" and hands focus back to the grid; Enter keeps the highlighted filter and also hands focus back to the grid (`focusGridMidViewport()`, shared by both) — landing on the Datum cell of whichever row is roughly mid-viewport (`getFirstDisplayedRowIndex`/`getLastDisplayedRowIndex`, deferred a tick so a filter change has actually re-rendered the row set first). Ctrl+K, arrow, Enter, Ctrl+K again is a closed loop back to the same button.
  - **Still not built:** the real tag-autocomplete mechanism, the quick-filter text box, per-field column filters, date-range presets, saved filters (§3a), and the auto-suggested transfer-leg matching (also §3a — creating one side of a transfer today just makes a single-sided row; nothing suggests pairing it with the other leg yet).
  - **Phone-specific condensed layout (§1b.7/§3a's compact card view) isn't built at all** — Konten today is the same grid+panel at every width, just reflowing. **Fixed, though (Sept 2026):** the page itself couldn't scroll on a narrow screen — `App.jsx`'s `main` was `overflow-hidden`, which worked while the pinned panel was short (tablet width, 3-4 columns) but trapped it unreachably once it stacked to four tall blocks on phone width. `main` is `overflow-y-auto` now, and the grid has a fixed height (`h-[70vh]`) instead of `flex-1`/`min-h-0` (which depended on a bounded parent height that no longer holds on every screen size).
  - **Known gap to watch:** `ag-grid-community`'s `AllCommunityModule` pulled the precached bundle from ~850 KB to ~2 MB (still under `vite-plugin-pwa`'s 6 MB cap, but worth revisiting — importing only the specific feature modules Konten actually uses, instead of the "all community features" bundle, would likely cut this significantly).
- `src/ImportScreen.jsx` — **temporary, Phase 1a's one-off data loader**, not a routed screen (no router yet). Lets Markus pick the JSON files `migration/`'s scripts generated (sent to him directly, never committed — see `migration/`'s own section above) and batch-writes them into Firestore via `writeBatch`, routing by filename into `accounts`/`categories`/`tags`/`transactions`/`budgets`. Every document id is deterministic, so re-running it is safe (last write of identical data wins). **Used successfully 2026-09-22** (2831 transactions, 2065 budget rows, 60 tags, 49 categories, 27 accounts — all counts matched what was sent). No longer `App.jsx`'s main content — reachable via a "Datenimport" toggle in the header instead (Markus's call: keep the code available for later, e.g. loading more years, rather than deleting it now that Konten is the default screen).
- `src/BackupScreen.jsx` — **PLAN.md Phase 1b's basic safety net (built Sept 2026)**, reachable via a "Sicherung" header toggle alongside "Datenimport" (independent of it — each toggles its own view, never both landing on "Zurück zu Konten" at once). Deliberately the rough version, not spec.md §3k's polished one (a separate parent/child transactions CSV, its own real nav slot, a documented restore procedure — that's Phase 7); this is just "does a real safety net exist before Phase 3's CSV import and later Settings' destructive operations start touching real data with no undo." (Phase 3, not Phase 2 — Sept 2026, Markus swapped Verlauf/Planung ahead of it.)
  - **Export**: reads every collection (`accounts`, `categories`, `tags`, `transactions`, `budgets`, `categoryYearSettings`, `settings` — the last two aren't read live by any built screen yet, but exist in Firestore since Phase 1a's seeding and belong in a "dump everything" backup regardless) via one-shot `getDocs` calls, not `onSnapshot` — this screen only needs a snapshot at the moment of the click, not a live subscription. Combined into **one JSON file**, not one per collection (§3k itself calls the file-count question an implementation detail, not a design decision) — simpler for Markus to keep track of. Downloaded via a plain `Blob`/`URL.createObjectURL`/anchor-click, named `geld-sicherung-<date>.json`.
  - **Restore**: picks that same file back, batch-writes each collection's array via `writeBatch` (chunked at 400, same as `ImportScreen`'s own `CHUNK_SIZE`) — a plain upsert keyed on each document's own `id`, never a wipe-and-replace, so it only ever brings lost/corrupted data back and never deletes something a partial backup file happens not to mention. Deliberately its own code, not routed through `ImportScreen`'s existing file-classification logic — that one enforces a legacy, migration-specific split (`tags-allocation.json` vs. `tags-breakdown-<year>.json`, `jahresabschluss.json` vs. `transactions-<year>.json`) that doesn't match what a live "dump every doc, as-is" export naturally produces, and forcing compatibility with it would have meant inheriting migration-era quirks for no real benefit.
  - **Verified via a disposable Playwright harness against a mocked `firebase/firestore`** (this one needed a real fix to the mock itself, not just the app: real Firestore's `doc()` supports both `doc(db, name, id)` — what `Konten.jsx` mostly uses — and `doc(collectionRef, id)` — what `ImportScreen`/`BackupScreen` both use — and the mock only implemented the first form until this round, silently misrouting every write `BackupScreen` made during its first test run). Confirmed a full round trip: export captures the exact seeded data; deliberately corrupting one document afterward, then restoring from the exported file, brings that document back to its original content exactly, with every other collection's counts matching too.

## Shared/pure calculation functions

- **`balance(accountId, asOfDate, transactions)`** — `src/lib/balance.js`, tested in `src/lib/balance.test.js` (`npm test`, run in CI before every deploy — see `.github/workflows/deploy.yml`). All-time running balance per spec.md §2.1/§2.6/§2.8: magnitude signed by the account's own position (`fromAccountId`/`toAccountId`), never the stored field's raw sign applied to both sides. Also exports `jahresanfang()`/`jahresende()`, the computed-lookup helpers from §2.1/§2.3.
- **`tagBalance(tagId, asOfDate, transactions, tags)`** — `src/lib/tagBalance.js`, tested in `src/lib/tagBalance.test.js`. The same computation, generalized to an allocation tag's own `reconciliationTargetAccountIds` set instead of one account id, and summed per *line* rather than per transaction (§2.6 — a tag lives on `lines[]`). Also exports `tagJahresende()`. Powers the pinned panel's allocation-tag rows (Konten.jsx).
- **`tagFilterTotal(tagId, asOfDate, transactions, aussenstaendeAccountId, tags)`** — same file, same tests. For a grouping tag (no stored target of its own, unlike an allocation tag): position-based sign against one hardcoded account (`aussenstaende`) when the tagged line's transaction touches it, natural sign when genuinely single-sided, excluded when it's a transfer between two other real accounts. Powers the panel's Außenstände open-claim rows and the toolbar's "Summe" readout (Konten.jsx).
- **`tagFilterMatchIds(tagId, tags)`** — same file, same tests (Sept 2026, Markus: "I need to be able to filter for the parent tag, too, and show the total: Schottland in Schottland:Whatever"). Returns the id set a filter/total for `tagId` actually matches against — itself plus every tag whose own `parentTag` is `tagId` — since only a *child* id is ever really applied to a line (`createTag()`'s own comment), matching a parent id alone would silently match nothing. One level deep only (matches the app's own hierarchy — a child never has children of its own). Shared by `tagFilterTotal()` itself and `Konten.jsx`'s `rows` filter predicate, so the grid's own row filtering and the toolbar's total can't disagree about what "filtering by Schottland" actually includes.
  - **The committed test suite uses synthetic fixtures, not Markus's real 2025 figures** — a deliberate deviation from PLAN.md Phase 1a's original wording ("tested against 2025's real migrated figures"), forced by the standing privacy rule (no real financial data in this public repo, ever). The equivalent real-data validation already happens, just not as a committed unit test: `migration/transform-transactions.py`'s `verify_account_sums()` implements this exact same per-account-position sign rule and checks it against the real Gsheet totals for every account, every year (CODEMAP.md's `migration/` section above) — and `Konten.jsx`'s pinned balance panel is itself a second, live confirmation of the same thing against real data, just never captured as a repo-committed test.
- **`withRemainder(amountCents, lines)`** — `src/lib/split.js`, tested in `src/lib/split.test.js` (PLAN.md Phase 1b's own called-out test-suite addition: "the split-transaction invariant"). Given a fixed parent total and a `lines` array with length > 1, returns a new array with every line but the last unchanged and the last recomputed as whatever's left — the pure core of `Konten.jsx`'s `persistTx`, which is the only caller and only invokes it once `lines.length > 1` (a ≤1-line transaction mirrors the parent's amount down into the line instead, a different, simpler rule that stays inline in `persistTx` rather than being extracted).

## Known duplication or drift to watch

- **The year selector lives inside `Konten.jsx` itself, not the app shell header** — spec.md §1b.2a specifies "a single year selector lives in the app shell header (not per-screen), and drives every year-scoped view." Right now there's only one real screen, so building it inside `Konten.jsx` (its own `years`/`setYear` state and the row of year buttons in its own JSX) hasn't caused any actual duplication yet — but it's a real divergence from the spec'd architecture, not just an implementation detail, and it's exactly the kind of thing that needs deliberate attention (not a copy-paste) when Verlauf (Phase 2, which also needs a year selector per spec.md §1b.2a) gets built alongside the real nav shell. Move it up into `App.jsx`'s header then, rather than adding a second independent one inside Verlauf.
